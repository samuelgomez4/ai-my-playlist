import './App.css'

function App() {
  const handleClick = () => {}
  return (
    <button
      type="button"
      onClick={handleClick}>
      {' '}
      Connect to Spotify
    </button>
  )
}

export default App
